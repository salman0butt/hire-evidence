# Iterations

An iteration is the unit Codex should implement in one focused development cycle. It should be small, dependency-ordered, coherent and independently verifiable.

Use `ITERATION-ROADMAP.md` as the default decomposition and `templates/ITERATION.md` when a dedicated iteration file is useful. The active iteration must always be recorded in `docs/milestones/CURRENT.md`.
