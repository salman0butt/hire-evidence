export default function AppPage() {
  return (
    <section aria-labelledby="workspace-heading" className="space-y-4">
      <p className="text-sm font-semibold uppercase tracking-[0.18em] text-zinc-500">
        Workspace
      </p>
      <h1 id="workspace-heading" className="text-3xl font-semibold tracking-tight">
        Interview workspace
      </h1>
      <p className="max-w-2xl text-zinc-600">
        Your authenticated workspace is ready. Organization setup and interview-building tools arrive in the milestones that own those capabilities.
      </p>
    </section>
  );
}
